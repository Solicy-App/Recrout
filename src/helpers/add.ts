// ATTENTION
// This function is only used for test. It doesn't do anything with the main codebase.
// Delete it after setting up the project.
const add = (a: number, b: number): number => {
  return a + b;
};

export default add;
